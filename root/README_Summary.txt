

1. The code works through 3 class. The packet class deciphers the byte structure of the PCAP file and turns
it into something usable. Brook handles all the packets and answers most of the actual questions.
FlowControl handles the TCP flows. 

3. Enter 'y' to run a custom pcap file otherwise type anything else
to run the default analysis